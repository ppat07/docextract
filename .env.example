ANTHROPIC_API_KEY=sk-ant-...
DOCEXTRACT_API_KEYS=key1,key2,key3

# Stripe (optional — billing is stubbed when not set)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=

# Database (SQLite path — defaults to /data/docextract.db in Docker)
DOCEXTRACT_DB_PATH=/data/docextract.db
